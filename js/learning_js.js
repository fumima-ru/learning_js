// jQuery readyイベント
$(document).ready(function() {
  console.log('HTML(DOM)の読み込みが完了したので処理を開始します');
});

// jQuery readyイベント省略形
$(function() {
  console.log('HTML(DOM)の読み込みが完了したので処理を開始します');
});

// 通常はHTML(DOM)読み込み完了後に1度だけ発生させる